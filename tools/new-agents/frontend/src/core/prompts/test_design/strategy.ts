import { FENCE } from '../../utils/constants';

export const STRATEGY_PROMPT = `基于需求澄清阶段的结论，制定测试策略蓝图并在右侧生成《测试策略蓝图》。
【重要警告】：产出物中绝对不要包含"下一步计划"或类似章节。
策略必须从澄清阶段的事实、业务规则、异常链路、风险种子和测试点候选出发，不要泛泛罗列测试类型。
风险分析必须同时输出 Mermaid quadrantChart 和 ai4se-visual risk-board。risk-board 用于稳定展示 FMEA 三因子、RPN、缓解策略和测试覆盖建议。
Mermaid 必须严格按模板格式输出：quadrantChart 的 title、x-axis、y-axis、quadrant-* 各自独立成行，轴标签和象限标签必须使用英文双引号；block-beta 只使用 columns 与普通节点，不要使用 block["..."] { ... } 分组语法。
`;

export const STRATEGY_TEMPLATE = `# 测试策略蓝图

## 1. 策略摘要

| 字段 | 内容 |
|---|---|
| 策略结论 | [1-3 句话说明本轮测试策略，例如风险驱动、主链路优先、异常链路强化] |
| 策略依据 | [引用 CLARIFY 中的事实 ID、规则 ID、链路 ID 或待确认问题 ID] |
| 进入用例阶段判断 | 可进入 / 暂缓进入 / 需补充策略输入 |

## 2. 质量目标

| 目标 ID | 质量目标 | 可验证指标 | 来源 | 优先级 | 状态 |
|---|---|---|---|---|---|
| QG-001 | [例如核心链路正确性、异常可恢复、安全边界等] | [通过率、响应时间、覆盖率、缺陷拦截目标] | F-xxx / BR-xxx / PATH-xxx | P0/P1/P2 | 已确认 / AI 假设 / 待确认 |

## 3. 风险识别与 FMEA

> FMEA (失效模式与影响分析) 标准三因子模型：
> - **严重度 S**：失效发生后影响有多大？(1=几乎无影响, 5=灾难性)
> - **发生度 O**：失效发生的可能性？(1=极不可能, 5=几乎必然)
> - **检出度 D**：现有测试/监控能在上线前发现该缺陷的概率？(1=必然检出, 5=几乎无法检出)
> - **RPN = S × O × D**（满分 125），RPN >= 60 为高风险需优先覆盖

### 3.1 风险矩阵
${FENCE}mermaid
quadrantChart
    title 风险优先级矩阵
    x-axis "低发生度" --> "高发生度"
    y-axis "低严重度" --> "高严重度"
    quadrant-1 "紧急处理"
    quadrant-2 "重点关注"
    quadrant-3 "观察监控"
    quadrant-4 "常规覆盖"
    "[风险名称1]": [发生度0-1, 严重度0-1]
    "[风险名称2]": [发生度0-1, 严重度0-1]
${FENCE}

### 3.2 风险明细
| 风险 ID | 风险名称 | 失效模式 | 影响 | 来源 | 严重度 S(1-5) | 发生度 O(1-5) | 检出度 D(1-5) | RPN(SxOxD) | 缓解策略 | 覆盖建议 | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| R-001 | [名称] | [模式] | [影响] | F-xxx / BR-xxx / PATH-xxx | [S] | [O] | [D] | [S×O×D] | [策略] | [对应 P0/P1 测试点或用例方向] | 待覆盖 / 已覆盖 / 风险接受 |

${FENCE}ai4se-visual
{
  "type": "risk-board",
  "title": "FMEA 风险处置看板",
  "columns": ["风险", "S", "O", "D", "RPN", "缓解策略", "覆盖建议"],
  "rows": [
    {
      "风险": "[风险名称]",
      "S": "[1-5]",
      "O": "[1-5]",
      "D": "[1-5]",
      "RPN": "[S×O×D]",
      "缓解策略": "[降低发生度或检出度的措施]",
      "覆盖建议": "[对应 P0/P1 测试点或用例方向]"
    }
  ]
}
${FENCE}

> RPN = S × O × D，RPN >= 60 为高风险，需优先覆盖。检出度 D 最易被改善（加强测试即可降低 D），是缓解策略的首要切入点。

## 4. 测试技术选型
| 技术 ID | 针对目标 | 技术类别 | 选用技术 | 选择理由 | 适用风险/测试点 |
|---|---|---|---|---|---|
| TS-001 | QG-xxx / R-xxx | 分析框架/设计技术/探索方法/自动化技术 | [技术] | [为什么适合当前风险] | R-xxx / TP-xxx |

## 5. 测试分层策略

### 5.1 测试金字塔
${FENCE}mermaid
block-beta
    columns 1
    e2e["E2E (占比%) - 端到端验证"]
    integ["集成 (占比%) - 接口与数据流验证"]
    unit["单元 (占比%) - 核心逻辑与边界值"]
${FENCE}

### 5.2 分层明细
| 层级 | 占比 | 覆盖范围 | 关联风险/测试点 | 推荐工具 | 进入条件 |
|---|---|---|---|---|---|
| 单元测试 | [%] | [范围] | R-xxx / TP-xxx | [工具] | [代码可测性、mock 条件] |
| 集成测试 | [%] | [范围] | R-xxx / TP-xxx | [工具] | [接口、数据、依赖条件] |
| E2E 测试 | [%] | [范围] | R-xxx / TP-xxx | [工具] | [环境、账号、数据条件] |

## 6. 测试点拓扑
| 测试点 ID | 测试点 | 优先级 | 关联质量目标 | 关联风险 | 测试技术 | 测试层级 | 预估用例数 | 覆盖建议 | 状态 |
|---|---|---|---|---|---|---|
| TP-001 | [测试点名称] | P0/P1/P2 | QG-xxx | R-xxx | TS-xxx [技术名称] | 单元/集成/E2E | [N] | [后续 CASES 的用例方向] | 待生成用例 / 已覆盖 |

> **覆盖统计**：共 [N] 个测试点，P0: [n1] 个 | P1: [n2] 个 | P2: [n3] 个，预估总用例 [M] 条

## 7. 资源与取舍

| 取舍项 | 决策 | 影响 | 需要确认人 | 状态 |
|---|---|---|---|---|
| [例如 E2E 覆盖比例、性能测试范围、第三方依赖 mock] | [保留/降级/暂缓] | [对风险覆盖和交付时间的影响] | 产品 / 研发 / 测试 / 用户确认 | 已确认 / 待确认 / AI 假设 |

## 8. 阶段门禁

- [ ] 所有 P0 风险都有覆盖建议。
- [ ] 测试点拓扑能追溯到质量目标、风险或澄清阶段输入。
- [ ] 用例阶段需要的数据、环境和依赖条件已列出。
- [ ] 资源取舍的影响和确认状态已写明。`;
