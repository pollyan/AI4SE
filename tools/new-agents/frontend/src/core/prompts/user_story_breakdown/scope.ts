import { FENCE } from '../../utils/constants';

export const SCOPE_PROMPT = `基于用户提供的需求蓝图、产品方向或核心需求，先校准哪些需求可以进入用户故事拆分。

引导流程：
1. 识别可拆分的需求范围，为每条需求分配稳定 requirementId，例如 REQ-001。
2. 区分本轮进入拆解的需求、不进入拆解的需求，以及仍被开放问题阻塞的需求。
3. 只讨论需求范围和业务边界，不输出开发任务、实现计划、代码结构、文件路径或测试命令。

【重要】：
- 需求 ID 必须稳定，后续故事卡只能引用这些需求 ID。
- 如果需求缺少用户、场景、验收标准或业务规则，要先标为阻塞问题。
- 不要把需求拆成“建表、写接口、做页面、写测试”这类技术横切任务。
- 必须包含 Mermaid flowchart，展示进入拆分、不拆范围和阻塞问题的关系。`;

export const SCOPE_TEMPLATE = `# 用户故事拆解文档

## 1. 拆分范围
| 需求 ID | 需求名称 | 用户价值 | 优先级 | 是否进入拆分 | 状态 |
| --- | --- | --- | --- | --- | --- |
| REQ-001 | [需求名称] | [用户获得的业务结果] | P0 / P1 / P2 | 是 / 否 | 已确认 / 待确认 / 阻塞 |

## 2. 需求追溯索引
| 需求 ID | 来源章节 / 上游依据 | 目标用户 | 关键场景 | 验收线索 | 状态 |
| --- | --- | --- | --- | --- | --- |
| REQ-001 | [需求蓝图章节或用户输入] | [用户类型] | [场景] | [可验证结果] | 已确认 / 待补充 |

## 3. 不拆范围
${FENCE}mermaid
flowchart TD
    INPUT["需求蓝图"] --> IN_SCOPE["进入拆分范围"]
    INPUT --> OUT_SCOPE["不拆范围"]
    INPUT --> BLOCKED["阻塞问题"]
    IN_SCOPE --> REQ001["REQ-001"]
${FENCE}

| 范围项 | 不拆原因 | 重新进入条件 | 状态 |
| --- | --- | --- | --- |
| [范围项] | [超出 MVP / 依赖不清 / 非本轮目标] | [何时再拆] | 已记录 / 待确认 |

## 4. 阻塞问题
| 问题 ID | 关联需求 ID | 问题 | 对拆分的影响 | 需要谁确认 | 状态 |
| --- | --- | --- | --- | --- | --- |
| Q-001 | REQ-001 | [需要补充的问题] | [缺少后无法写故事或验收标准] | 产品 / 业务 / 用户 | 开放 / 已确认 |

## 5. 阶段门禁
- [ ] 进入拆分的需求都有稳定需求 ID。
- [ ] P0 需求至少具备目标用户、关键场景和初步验收线索。
- [ ] 不拆范围已经说明原因，不会混入后续故事卡。
- [ ] 阻塞问题已经列出责任方和状态。
`;
