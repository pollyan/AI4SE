# Docker部署环境变量配置（只包含Web应用和数据库）
# MidScene Server手动启动，不在Docker中
# 复制此文件为 .env 使用: cp .env.docker.example .env

# ===========================================
# 数据库配置
# ===========================================
DB_USER=intent_user
DB_PASSWORD=local_dev_password

# ===========================================
# Flask应用配置
# ===========================================
SECRET_KEY=local-dev-secret-key-change-in-production
FLASK_ENV=development

# ===========================================
# 服务端口（默认值，一般不需要修改）
# ===========================================
# WEB_PORT=5001
# DB_PORT=5432
