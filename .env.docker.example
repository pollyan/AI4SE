# Docker部署环境变量配置
# 复制此文件为 .env 使用: cp .env.docker.example .env

# ===========================================
# 数据库配置
# ===========================================
# 数据库用户名（建议在生产环境修改）
DB_USER=intent_user

# 数据库密码（生产环境必须修改为强密码！）
DB_PASSWORD=change_me_in_production

# 注意事项：
# - 本地开发: PostgreSQL容器映射到 localhost:5432
# - 生产环境: PostgreSQL容器映射到 localhost:5433 (避免端口冲突)
# - 容器内部: web-app通过 postgres:5432 连接（Docker内部网络）

# ===========================================
# Flask应用配置
# ===========================================
# 应用密钥（生产环境必须修改为随机强密钥！）
SECRET_KEY=please-change-this-to-random-secret-in-production

# 运行环境（production/development）
FLASK_ENV=production

# ===========================================
# 服务端口（一般不需要修改）
# ===========================================
# Web应用端口
# WEB_PORT=5001

# 数据库宿主机端口（生产环境）
# DB_HOST_PORT=5433

# ===========================================
# 安全提醒
# ===========================================
# 在生产环境部署前，请确保修改：
# 1. DB_PASSWORD - 使用强随机密码
# 2. SECRET_KEY - 使用强随机密钥
# 3. FLASK_ENV - 设置为 production

