from __future__ import annotations

import json
from dataclasses import dataclass
from typing import Any


@dataclass(frozen=True)
class StagePayload:
    chat: str
    markdown: str
    next_stage_id: str | None = None
    clarification_chat: str | None = None
    turns_before_transition: int = 2


STAGE_PAYLOADS: dict[tuple[str, str], StagePayload] = {
    ("TEST_DESIGN", "CLARIFY"): StagePayload(
        chat="需求澄清完成，请确认进入策略制定。",
        next_stage_id="STRATEGY",
        markdown="""# 需求分析文档

## 1. 被测系统与边界
登录与支付联动能力是本次被测系统，覆盖账号密码登录、短信验证码登录、支付提交、失败重试和账号锁定。

## 2. 系统交互与核心链路
用户从登录页进入，完成身份校验后进入支付页，支付结果回写订单中心。

```mermaid
flowchart TD
    Login["登录页"] --> Auth["身份校验"]
    Auth --> Pay["支付提交"]
    Pay --> Order["订单中心状态回写"]
```

## 3. 待澄清与阻断性问题
| 问题 | 状态 |
| --- | --- |
| 账号锁定阈值是否为 5 次失败 | 已确认 |

## 4. 隐式需求与非功能性考量
需要覆盖并发登录、敏感信息脱敏、安全审计和移动端兼容性。
""",
    ),
    ("TEST_DESIGN", "STRATEGY"): StagePayload(
        chat="测试策略蓝图完成，请确认进入用例编写。",
        next_stage_id="CASES",
        markdown="""# 测试策略蓝图

## 1. 质量目标
- P0 主链路上线前全部通过。
- 高风险安全场景必须有自动化或专项验证。

## 2. 风险分析

### 2.1 风险矩阵
| 风险 | 优先级 |
| --- | --- |
| 登录绕过 | P0 |

```ai4se-visual
{"type":"risk-board","title":"风险处置看板","columns":["风险","等级","策略"],"rows":[["登录绕过","P0","安全专项"],["重复回调","P0","幂等校验"]]}
```

### 2.2 风险明细
| ID | 风险名称 | 缓解策略 |
| --- | --- | --- |
| R-001 | 账号锁定失效 | 覆盖连续失败和解锁流程 |

## 3. 测试技术选型
| ID | 技术 | 理由 |
| --- | --- | --- |
| TS-001 | API + E2E | 同时覆盖契约和主链路 |

## 4. 测试分层策略

### 4.1 测试金字塔
单元 50%，集成 30%，E2E 20%。

### 4.2 分层明细
| 层级 | 范围 |
| --- | --- |
| E2E | 登录到支付成功 |

## 5. 测试点拓扑
| ID | 测试点 | 优先级 |
| --- | --- | --- |
| TP-001 | 登录成功后支付 | P0 |
""",
    ),
    ("TEST_DESIGN", "CASES"): StagePayload(
        chat="测试用例集完成，请确认进入文档交付。",
        next_stage_id="DELIVERY",
        markdown="""# 测试用例集

## 1. 用例统计
共 6 条用例，P0 4 条，P1 2 条。

## 2. 用例清单
| ID | 用例标题 | 优先级 | 操作步骤 | 预期结果 |
| --- | --- | --- | --- | --- |
| TC-001 | 密码登录成功后支付 | P0 | 输入正确账号密码并提交支付 | 支付成功并生成订单 |
| TC-002 | 连续失败触发锁定 | P0 | 连续输入错误密码 | 账号被锁定并记录审计 |

## 3. 测试点覆盖追溯
| 测试点 | 覆盖用例 | 覆盖状态 |
| --- | --- | --- |
| 登录成功后支付 | TC-001 | 已覆盖 |

```ai4se-visual
{"type":"traceability-matrix","title":"测试覆盖追溯矩阵","columns":["测试点","用例","状态"],"rows":[["登录成功后支付","TC-001","已覆盖"],["连续失败触发锁定","TC-002","已覆盖"]]}
```
""",
    ),
    ("TEST_DESIGN", "DELIVERY"): StagePayload(
        chat="测试设计交付文档已完成。",
        markdown="""# 测试设计文档

## 文档信息
| 字段 | 内容 |
| --- | --- |
| 项目名称 | 登录支付链路测试设计 |
| 测试范围 | 账号密码登录、短信验证码登录、第三方登录入口、微信支付、银行卡支付、订单状态回写 |
| 不测范围 | 第三方登录渠道底层稳定性、微信和银行通道自身可用性 |
| 质量目标 | P0 主链路 100% 通过；高风险安全与幂等场景全部有验证记录 |

## 第一部分：需求分析
### 1. 被测系统与边界
本次验证用户完成身份校验后发起支付、接收支付回调并更新订单状态的完整链路。登录方式包括账号密码、短信验证码和第三方登录；支付方式只覆盖微信和银行卡。第三方渠道和支付通道底层稳定性通过契约与异常回调模拟验证，不直接压测外部服务。

### 2. 核心业务规则
| 规则 ID | 规则 | 验证重点 |
| --- | --- | --- |
| BR-001 | 密码连续失败 5 次后账号锁定 30 分钟 | 锁定触发、锁定期间拦截、解锁后恢复 |
| BR-002 | 短信验证码 60 秒内不能重复发送 | 倒计时展示、重复点击拦截、接口防刷 |
| BR-003 | 支付回调可能超时、重复或乱序 | 订单状态一致性、回调幂等、审计日志 |
| BR-004 | 登录成功态是支付发起前置条件 | 未登录拦截、登录态过期、跨端会话冲突 |

### 3. 隐式需求和非功能要求
需要覆盖敏感信息脱敏、安全审计、移动端兼容性、弱网重试、并发支付提交和异常提示可理解性。所有 P0 场景需要能追溯到需求规则和风险点。

## 第二部分：测试策略
### 1. 优先级策略
| 优先级 | 范围 | 准出标准 |
| --- | --- | --- |
| P0 | 登录成功后支付、支付成功回调、订单状态更新、账号锁定、重复回调幂等 | 全部通过且无阻断缺陷 |
| P1 | 支付失败重试、短信验证码限流、第三方登录入口异常、银行卡绑卡状态异常 | 主要路径通过，遗留缺陷有规避方案 |
| P2 | 文案、低频兼容性、导出和审计辅助信息 | 不影响主流程上线 |

### 2. 测试分层
API 层验证登录、支付、订单和回调契约；E2E 层覆盖用户可见主链路；专项测试覆盖安全审计、幂等、弱网和并发提交。回归时先跑 P0 API 契约，再跑 4 条核心 E2E，最后补齐 P1 风险用例。

### 3. 风险矩阵
| 风险 | 影响 | 概率 | 策略 |
| --- | --- | --- | --- |
| 支付成功但订单未更新 | 高 | 中 | 构造回调超时和重复回调，检查最终一致性 |
| 账号锁定失效导致撞库风险 | 高 | 中 | 连续失败、锁定期间登录、解锁恢复三段验证 |
| 重复点击支付造成重复扣款 | 高 | 低 | 并发提交、按钮防抖、服务端幂等校验 |
| 登录态过期后仍可支付 | 高 | 中 | 会话过期、跨端登录、未登录直接支付验证 |

## 第三部分：测试用例
### 1. 正向主链路
| ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| TC-001 | 账号密码登录后微信支付成功 | 用户未登录，商品可购买 | 输入正确账号密码；进入订单页；选择微信支付；模拟成功回调 | 登录成功，支付单创建，订单状态更新为已支付，生成审计日志 | P0 |
| TC-002 | 短信验证码登录后银行卡支付成功 | 手机号已绑定银行卡 | 获取验证码；输入正确验证码；选择银行卡支付；确认支付 | 验证码只能使用一次，支付成功后订单金额和支付流水一致 | P0 |

### 2. 异常和幂等
| ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| TC-003 | 支付回调超时后重试成功 | 订单已创建 | 模拟首次回调超时；再次发送成功回调 | 订单最终为已支付，仅记录一条有效支付结果 | P0 |
| TC-004 | 重复支付回调不重复更新订单 | 订单已支付 | 连续发送两次相同成功回调 | 第二次回调返回幂等成功，订单金额和流水不重复 | P0 |
| TC-005 | 重复点击支付提交不重复扣款 | 订单待支付 | 在支付按钮可点击瞬间连续点击两次，并模拟服务端收到并发支付请求 | 前端按钮防重复提交，服务端只创建一笔支付流水，第二次请求返回幂等结果 | P0 |
| TC-006 | 支付失败后用户重新发起支付 | 支付通道返回失败 | 发起支付；模拟失败；重新选择支付方式 | 首次失败有明确提示，重试成功后订单状态正确 | P1 |

### 3. 安全和兼容
| ID | 标题 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
| --- | --- | --- | --- | --- | --- |
| TC-007 | 密码连续失败 5 次触发锁定 | 用户存在且未锁定 | 连续 5 次输入错误密码；第 6 次输入正确密码 | 账号锁定 30 分钟，正确密码也不能登录，审计记录完整 | P0 |
| TC-008 | 短信验证码 60 秒内重复发送被拦截 | 手机号可接收短信 | 点击发送验证码后 60 秒内再次点击 | 前端按钮置灰，后端拒绝重复发送请求 | P1 |
| TC-009 | 移动端支付页展示和提交一致 | 使用移动端视口 | 在移动端完成登录和支付提交 | 关键按钮可见，金额、订单号和状态展示不截断 | P2 |

### 4. 覆盖追溯
| 需求/风险 | 覆盖用例 |
| --- | --- |
| 登录成功后支付 | TC-001, TC-002 |
| 回调超时和重复回调 | TC-003, TC-004 |
| 重复提交和重复扣款风险 | TC-005 |
| 账号锁定和短信限流 | TC-007, TC-008 |
| 兼容性 | TC-009 |

```ai4se-visual
{"type":"coverage-map","title":"交付覆盖图","columns":["需求/风险","用例","验收状态"],"rows":[["登录成功后支付","TC-001, TC-002","通过"],["回调超时和重复回调","TC-003, TC-004","通过"],["账号锁定和短信限流","TC-007, TC-008","待执行"]]}
```

## 附录：验收标准
- 所有 P0 用例通过。
- 高风险项均有对应缓解策略。
- P1 失败项必须有明确上线规避方案和负责人。
- 回归报告需要包含需求覆盖表、缺陷列表和未测风险声明。
""",
    ),
    ("VALUE_DISCOVERY", "ELEVATOR"): StagePayload(
        chat="价值定位分析完成，请确认进入用户画像。",
        next_stage_id="PERSONA",
        markdown="""# 价值定位分析

## 产品核心定位
面向测试团队的 AI 测试设计助手，帮助从需求快速生成测试策略和用例。

## 目标用户概览
| 维度 | 描述 |
| --- | --- |
| 主要用户群体 | 中大型软件团队的测试负责人 |

## 独特价值主张
| 维度 | 我们 | 现有方案/竞品 |
| --- | --- | --- |
| 核心优势 | 结构化引导并沉淀产物 | 依赖人工经验 |

## 商业可行性初判
| 维度 | 判断 |
| --- | --- |
| 用户付费意愿 | 对节省测试设计时间有明确预算 |

```ai4se-visual
{"type":"score-matrix","title":"价值评分矩阵","columns":["维度","评分","依据"],"rows":[["痛点强度","90","测试设计耗时且容易遗漏"],["付费意愿","82","测试负责人有明确效率预算"],["差异化","78","结构化引导与资产沉淀"]]}
```

## 60 秒电梯演讲
我们帮助测试团队把模糊需求快速转化为可执行测试资产。
""",
    ),
    ("VALUE_DISCOVERY", "PERSONA"): StagePayload(
        chat="用户画像分析完成，请确认进入用户旅程。",
        next_stage_id="JOURNEY",
        markdown="""# 用户画像分析

## 主要用户画像

### 画像 1：测试负责人

#### 基础特征
管理 5 到 20 人测试团队，关注质量风险和交付效率。

#### 行为特征
经常参与需求评审、测试计划制定和上线风险评估。

#### 需求动机
希望减少重复文档工作，同时提高风险识别完整度。

#### 核心痛点
| 痛点 | 频率 | 影响程度 | 现有方案不足 |
| --- | --- | --- | --- |
| 用例设计耗时 | 每周 | 高 | 人工依赖强 |

## 用户优先级排序
| 优先级 | 用户类型 | 理由 |
| --- | --- | --- |
| 核心用户 | 测试负责人 | 直接承担质量交付责任 |
""",
    ),
    ("VALUE_DISCOVERY", "JOURNEY"): StagePayload(
        chat="用户旅程分析完成，请确认进入需求蓝图。",
        next_stage_id="BLUEPRINT",
        markdown="""# 用户旅程分析

## 用户旅程地图
从需求输入到策略制定、用例编写、评审交付。

```ai4se-visual
{"type":"journey-map","title":"用户旅程图","columns":["阶段","用户任务","主要痛点","产品机会"],"rows":[["需求评审","识别风险","信息不完整","结构化澄清"],["策略制定","确定优先级","依赖个人经验","风险矩阵"],["评审交付","统一材料","追溯困难","覆盖矩阵"]]}
```

## 关键阶段详细分析
| 阶段 | 用户目标 | 主要痛点 |
| --- | --- | --- |
| 需求评审 | 找出风险 | 信息不完整 |

## 痛点优先级排序

### 高优先级痛点（必须解决）
测试设计耗时且遗漏风险。

### 中等优先级痛点（应该解决）
用例格式和覆盖追溯不统一。

### 低优先级痛点（可以解决）
导出格式需要适配团队模板。

## 核心机会点

### 主要机会点
用结构化智能体引导测试设计。

### 产品切入策略
优先切入需求评审后的测试策略和用例生成环节。
""",
    ),
    ("VALUE_DISCOVERY", "BLUEPRINT"): StagePayload(
        chat="需求蓝图已完成。",
        markdown="""# AI 测试设计助手需求蓝图

## 文档信息
| 维度 | 内容 |
| --- | --- |
| 产品方向 | AI 辅助测试设计 |

## 1. 产品概述

### 1.1 产品愿景
让测试团队更快识别风险并沉淀高质量测试资产。

### 1.2 定位声明
For 测试负责人 who 需要快速完成测试设计，the AI 测试设计助手 is a 结构化智能体工具 that 生成策略和用例。

### 1.3 核心价值
| 价值维度 | 具体收益 | 可验证指标 |
| --- | --- | --- |
| 效率 | 将需求评审后的测试设计初稿从半天压缩到 30 分钟内 | 单个需求测试设计耗时降低 30% 以上 |
| 质量 | 用结构化问题和风险库减少遗漏 | P0 风险覆盖率达到 95% |
| 协作 | 输出统一格式的策略、用例和追溯关系 | 评审返工次数下降 |
| 沉淀 | 将历史项目经验转化为团队资产 | 模板复用率和历史用例引用率提升 |

## 2. 目标用户（摘要）
### 2.1 核心用户
10 人以上测试团队的测试负责人。他们每天参加需求评审，每周需要交付测试计划、测试用例和上线风险说明，最关心输出质量、可控性、权限隔离和现有需求系统接入。

### 2.2 关键使用者
资深测试工程师负责补充业务规则、校验 AI 产物和执行用例评审；研发负责人关注风险是否透明、上线准出是否可靠；产品经理关注需求变更后测试影响是否及时反馈。

### 2.3 用户痛点
| 痛点 | 当前表现 | 产品机会 |
| --- | --- | --- |
| 需求信息不完整 | 测试负责人需要反复追问业务规则 | 通过澄清问题清单暴露阻断点 |
| 测试点遗漏 | 依赖个人经验，评审后才发现漏测 | 风险库和追溯矩阵自动提示 |
| 用例交付慢 | 文档格式重复、颗粒度不一致 | 分阶段生成策略和用例资产 |

## 3. 核心需求

### 功能架构
需求输入、澄清问答、风险分析、策略生成、用例生成、覆盖追溯、评审协作、交付导出和历史资产复用。

### P0 需求（核心功能，必须实现）
| ID | 需求名称 | 用户价值 | 验收标准 |
| --- | --- | --- | --- |
| F-001 | 需求澄清与风险识别 | 在设计前发现缺失业务规则 | 能输出待澄清问题、隐式需求和 P0 风险清单 |
| F-002 | 生成测试策略 | 给负责人可评审的执行方案 | 包含质量目标、分层策略、风险矩阵和准出标准 |
| F-003 | 生成测试用例 | 降低人工编写成本 | 用例包含前置条件、步骤、预期结果、优先级和覆盖追溯 |
| F-004 | 产物版本和导出 | 便于团队评审和沉淀 | 支持 Markdown 交付和阶段产物历史版本 |

### P1 需求（重要功能，应该实现）
| ID | 需求名称 | 价值 |
| --- | --- | --- |
| F-101 | 团队模板适配 | 输出符合公司既有评审格式 |
| F-102 | 需求系统接入 | 从需求文档或工单自动读取上下文 |
| F-103 | 权限和空间隔离 | 满足团队协作和数据安全要求 |

### P2 需求（增值功能，可以实现）
历史项目复用、缺陷数据反哺风险库、自动生成评审汇报和多语言导出。

## 4. 核心流程

### 主流程图
用户输入需求后，系统生成多阶段测试设计产物。

### 4.1 关键用户旅程
1. 测试负责人导入需求或粘贴需求摘要。
2. 系统生成澄清问题，用户补充边界、业务规则和外部依赖。
3. 系统输出风险矩阵和测试策略，用户确认优先级和准出标准。
4. 系统生成分组用例和覆盖追溯表，资深测试工程师补充细节。
5. 团队导出交付文档进入评审，并把确认后的资产沉淀为模板。

### 4.2 交互原则
AI 必须分阶段输出，避免一次性生成不可控大文档；每次阶段切换需要用户确认；所有用例都能追溯到需求、风险或业务规则。

## 5. 成功指标
| 指标类型 | 指标名称 | 目标值 |
| --- | --- | --- |
| 效率指标 | 测试设计耗时降低 | 30% |
| 质量指标 | P0 风险覆盖率 | 95% |
| 采用指标 | 周活跃测试负责人 | 首批试点团队达到 60% |
| 商业指标 | 团队订阅转化率 | 试用到付费转化 20% |
| 协作指标 | 评审返工次数 | 降低 25% |

## 6. MVP 范围与计划

### MVP 包含功能
- F-001: 需求澄清与风险识别
- F-002: 生成测试策略
- F-003: 生成测试用例
- F-004: Markdown 交付导出

### 迭代路线
| 版本 | 核心功能 | 验证目标 |
| --- | --- | --- |
| v0.1 | 单工作流策略和用例生成 | 验证输出质量和用户节省时间 |
| v0.2 | 模板适配和版本历史 | 验证团队评审协作效率 |
| v1.0 | 需求系统接入和权限隔离 | 验证企业团队可部署性 |

```ai4se-visual
{"type":"roadmap","title":"MVP 路线图","columns":["版本","核心功能","验证目标"],"rows":[["v0.1","单工作流策略和用例生成","输出质量和节省时间"],["v0.2","模板适配和版本历史","评审协作效率"],["v1.0","需求系统接入和权限隔离","企业部署"]]}
```

## 7. 风险评估
| 风险类型 | 风险描述 | 缓解措施 |
| --- | --- | --- |
| 产品风险 | AI 输出不稳定或过于泛化 | 阶段契约校验、必填章节、人工确认和 LLM judge 质量门 |
| 用户风险 | 测试负责人不信任生成内容 | 保留追溯依据、展示风险来源、支持逐条编辑 |
| 集成风险 | 接入现有需求系统成本高 | MVP 先支持复制粘贴和 Markdown 导入，后续再做 API 集成 |
| 安全风险 | 需求文档包含敏感信息 | 提供私有化部署、空间隔离和敏感字段脱敏 |
| 商业风险 | 节省时间不足以支撑付费 | 用试点指标量化节省时间和漏测风险降低 |
""",
    ),
}


HANDOFF_TEST_DESIGN_STAGE_PAYLOADS: dict[tuple[str, str], StagePayload] = {
    ("TEST_DESIGN", "CLARIFY"): StagePayload(
        chat="已基于 Alex 需求蓝图完成测试设计需求澄清，请确认进入策略制定。",
        next_stage_id="STRATEGY",
        markdown="""# 需求分析文档

## 1. 被测系统与边界
被测系统是 AI 测试设计助手，核心能力包括需求输入、澄清问答、风险识别、测试策略生成、用例生成、覆盖追溯、阶段确认和 Markdown 交付。

## 2. 系统交互与核心链路
用户从需求输入开始，系统生成澄清问题和风险线索，用户确认后进入策略制定，再生成测试点、用例和覆盖追溯。

```mermaid
flowchart TD
    Input["需求输入"] --> Clarify["澄清问答"]
    Clarify --> Risk["风险识别"]
    Risk --> Strategy["测试策略"]
    Strategy --> Cases["用例与覆盖追溯"]
```

## 3. 待澄清与阻断性问题
| 问题 | 状态 |
| --- | --- |
| 是否需要接入现有需求系统 | 已作为 P1 集成风险记录 |
| 是否要求权限和空间隔离 | 已纳入安全与企业部署测试范围 |

## 4. 隐式需求与非功能性考量
需要覆盖 AI 输出质量、结构化契约稳定性、权限隔离、需求追溯、LLM judge 质量门、模板适配和异常恢复。
""",
    ),
    ("TEST_DESIGN", "STRATEGY"): StagePayload(
        chat="已把 Alex 蓝图转换为测试策略蓝图，请确认进入用例编写。",
        next_stage_id="CASES",
        markdown="""# 测试策略蓝图

## 1. 质量目标
- P0 覆盖需求澄清、策略生成、用例生成和覆盖追溯主链路。
- AI 输出必须满足结构化契约、可视化契约和 LLM judge 质量门。
- 权限隔离和敏感需求内容不得跨空间泄漏。

## 2. 风险分析

### 2.1 风险矩阵
| 风险 | 优先级 |
| --- | --- |
| AI 输出不稳定或过于泛化 | P0 |
| 用户不信任生成内容 | P0 |
| 接入现有需求系统成本高 | P1 |
| 需求文档包含敏感信息 | P0 |

```ai4se-visual
{"type":"risk-board","title":"AI 测试设计助手风险处置看板","columns":["Alex 风险","等级","Lisa 测试策略"],"rows":[["AI 输出不稳定","P0","契约校验 + LLM judge"],["用户不信任","P0","追溯依据和可编辑产物"],["敏感信息泄漏","P0","权限隔离和脱敏验证"]]}
```

### 2.2 风险明细
| ID | 风险名称 | 缓解策略 |
| --- | --- | --- |
| R-001 | 结构化输出失败 | 覆盖重试、错误提示和产物保持不变 |
| R-002 | 可视化无法渲染 | 覆盖 Mermaid 和 ai4se-visual fallback |
| R-003 | 权限越界 | 覆盖跨空间访问、分享边界和审计记录 |

## 3. 测试技术选型
| ID | 技术 | 理由 |
| --- | --- | --- |
| TS-001 | API 契约测试 | 验证 Agent Runtime 输出结构 |
| TS-002 | 浏览器 E2E | 验证阶段推进、产物渲染和 handoff 连续性 |
| TS-003 | LLM judge | 评估专业性、可视化质量和接力质量 |

## 4. 测试分层策略
单元与契约测试覆盖解析和状态机，E2E 覆盖 Lisa/Alex 共享工作流，LLM judge 覆盖专业质量门。

## 5. 测试点拓扑
| ID | 测试点 | 优先级 |
| --- | --- | --- |
| TP-001 | 从 Alex 需求蓝图生成 Lisa 测试设计 | P0 |
| TP-002 | 可视化契约被保留并可断言 | P0 |
| TP-003 | 结构化输出失败可重试且不污染产物 | P0 |
""",
    ),
    ("TEST_DESIGN", "CASES"): StagePayload(
        chat="已完成基于 Alex 蓝图的测试用例集。",
        markdown="""# 测试用例集

## 1. 用例统计
共 7 条用例，P0 5 条，P1 2 条，全部追溯到 Alex 蓝图中的核心需求或风险。

## 2. 用例清单
| ID | 用例标题 | 优先级 | 操作步骤 | 预期结果 |
| --- | --- | --- | --- | --- |
| TC-001 | 从模糊需求生成澄清问题 | P0 | 输入 AI 测试设计助手需求摘要 | 输出待澄清问题、隐式需求和风险线索 |
| TC-002 | 确认后生成测试策略蓝图 | P0 | 补充边界并确认进入策略制定 | 生成质量目标、风险矩阵、分层策略和测试点拓扑 |
| TC-003 | 生成用例并保留覆盖追溯 | P0 | 确认进入用例编写 | 每条用例包含前置条件、步骤、预期结果和追溯关系 |
| TC-004 | 结构化输出失败后支持重试 | P0 | 模拟 artifact_update 契约失败 | 左侧提示可重试，右侧旧产物保持不变 |
| TC-005 | 可视化契约渲染和 fallback | P0 | 输出 Mermaid 与 ai4se-visual | 前端展示图表或明确 fallback，不阻断阶段推进 |
| TC-006 | 权限隔离验证 | P0 | 尝试跨空间读取其他团队需求 | 请求被拒绝并记录审计 |
| TC-007 | 模板适配验证 | P1 | 选择团队模板导出 | Markdown 结构符合模板标题和表格要求 |

## 3. 测试点覆盖追溯
| 测试点 | 覆盖用例 | 覆盖状态 |
| --- | --- | --- |
| Alex 蓝图继承 | TC-001, TC-002, TC-003 | 已覆盖 |
| 输出质量和 LLM judge | TC-002, TC-003 | 已覆盖 |
| 结构化失败重试 | TC-004 | 已覆盖 |
| 可视化质量 | TC-005 | 已覆盖 |
| 权限隔离 | TC-006 | 已覆盖 |

```ai4se-visual
{"type":"traceability-matrix","title":"Alex 到 Lisa 覆盖追溯矩阵","columns":["Alex 蓝图要点","Lisa 测试点","用例"],"rows":[["AI 输出不稳定","结构化契约和 LLM judge","TC-002, TC-004"],["用户信任不足","覆盖追溯和可视化质量","TC-003, TC-005"],["权限隔离","跨空间访问控制","TC-006"]]}
```
""",
    ),
}


def build_agent_sse_response(
    request_body: dict[str, Any],
    *,
    turn_index: int = 0,
) -> str:
    workflow_id = request_body["workflowId"]
    stage_id = request_body["stageId"]
    run_id = request_body.get("runId")
    payloads = (
        HANDOFF_TEST_DESIGN_STAGE_PAYLOADS
        if run_id == "mock-run-test_design-handoff"
        else STAGE_PAYLOADS
    )
    payload = payloads[(workflow_id, stage_id)]
    should_request_next_stage = (
        payload.next_stage_id is not None
        and turn_index >= payload.turns_before_transition
    )
    output: dict[str, Any] = {
        "chat": (
            payload.chat
            if should_request_next_stage or not payload.next_stage_id
            else payload.clarification_chat
            or "当前阶段产出物已更新。我还需要你补充确认关键信息后，才会建议进入下一阶段。"
        ),
        "artifact_update": {
            "type": "replace",
            "markdown": payload.markdown,
        },
        "stage_action": None,
        "warnings": [],
    }
    if should_request_next_stage:
        output["stage_action"] = {
            "type": "request_next_stage",
            "target_stage_id": payload.next_stage_id,
        }
    run_started_event = {
        "type": "run_started",
        "runId": request_body.get("runId") or f"mock-run-{workflow_id.lower()}",
        "warnings": [],
    }
    event = {"type": "agent_turn", "output": output}
    return (
        f"data: {json.dumps(run_started_event, ensure_ascii=False)}\n\n"
        f"data: {json.dumps(event, ensure_ascii=False)}\n\n"
        "data: [DONE]\n\n"
    )
